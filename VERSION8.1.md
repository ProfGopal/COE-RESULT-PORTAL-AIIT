# Release Notes: AIIT Version 8.1

* **Curriculum Editor Dropdown Fix:** `window.loadCurriculumEditor()` populates curriculum options dynamically from `AIIT_SYSTEM_PROGRAMS` / `window.SYSTEM_PROGRAMS`.
* **Faculty Dashboard Whitespace Gap Removal:** Restructured `faculty-dash` container styling to eliminate whitespace gaps and auto-populate student directory filters.
* **Student Portal Logout Streamlining:** Standardized bottom logout button in student dashboard to display strictly `LOGOUT` and trigger `window.logoutPortal()`.
