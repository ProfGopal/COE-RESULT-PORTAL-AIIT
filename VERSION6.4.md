# Release Notes: AIIT Version 6.4

* **Direct Action Bindings:** Bound `onclick` actions directly to Faculty Sign-In button (`window.facultyLoginStep()`) and Admin Clear Password button (`window.clearStudentPassword('reset-sen-input')`).
* **Direct Event Handlers:** Added robust global `facultyLoginStep` and `clearStudentPassword` handler functions in `script.js`.
