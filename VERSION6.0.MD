# Release Notes: AIIT Version 6.0

* **Cloud Authorization Fix:** Admin password clearing requests now correctly pass session credentials (`adminPassword`), eliminating the "Unauthorized" popup error.
* **Dynamic Batch & Program Rendering:** `renderSystemPrograms()` now auto-initializes and displays active system tags on page load.
* **Tab Navigation Engine:** Robust selectors ensure smooth tab switching across all admin panels.